django:
  build: .
  restart: always
  ports:
    - 443:8000
  volumes:
    -  /home/marcel/scert:/etc/cert
  environment:
    - GRUNT_KEY=/etc/cert/key.pem
    - GRUNT_CERT=/etc/cert/cert.pem
    - DOCKYARD_SRC=/home/marcel/Wedding

